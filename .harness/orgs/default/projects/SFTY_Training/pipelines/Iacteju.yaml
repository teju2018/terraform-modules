pipeline:
  name: Iac-teju
  identifier: Iacteju
  projectIdentifier: SFTY_Training
  orgIdentifier: default
  tags: {}
  stages:
    - stage:
        name: vm
        identifier: vm
        description: ""
        type: IACM
        spec:
          platform:
            os: Linux
            arch: Amd64
          runtime:
            type: Cloud
            spec: {}
          workspace: TejPaulSpaceProvision
          execution:
            steps:
              - step:
                  type: IACMTerraformPlugin
                  name: init
                  identifier: init
                  timeout: 10m
                  spec:
                    command: init
              - step:
                  type: IACMTerraformPlugin
                  name: plan
                  identifier: plan
                  timeout: 10m
                  spec:
                    command: plan
              - parallel:
                  - step:
                      type: IACMTerraformPlugin
                      name: apply
                      identifier: apply
                      timeout: 1h
                      spec:
                        command: apply
                      when:
                        stageStatus: Success
                        condition: <+pipeline.variables.im>=="apply"
                  - step:
                      type: IACMTerraformPlugin
                      name: IACMTerraformPlugin_4
                      identifier: IACMTerraformPlugin_4
                      spec:
                        command: destroy
                      timeout: 1h
                      when:
                        stageStatus: Success
                        condition: <+pipeline.variables.im>=="destroy"
        tags: {}
  variables:
    - name: im
      type: String
      description: ""
      required: false
      value: <+input>.default(apply).allowedValues(apply,destroy)
